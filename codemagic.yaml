workflows:
  shikhaya-android:
    name: Shikhaya Android Build

    environment:
      flutter: stable

    scripts:
      - name: Unzip source
        script: |
          unzip -o shikhaya_app_source.zip

      - name: Create Android project
        script: |
          cd shikhaya
          flutter create . --platforms=android

      - name: Copy Firebase options
        script: |
          cp ../firebase_options.dart lib/firebase_options.dart

      - name: Install packages
        script: |
          flutter pub get

      - name: Build APK
        script: |
          flutter build apk --release

    artifacts:
      - shikhaya/build/**/outputs/**/*.apk
